# Acceptance evidence and remaining scope

This is a partial requirement audit, not a completion declaration. Checked PLAN.md items are supported below; unchecked items remain open until their full stated scope is demonstrated.

| Requirement | Current evidence | Status |
| --- | --- | --- |
| Window invariants and recipient/window snapshots | `tests/unit/test_delivery_domain.py`: 7 tests passed on 2026-09-06. `tools/delivery/orders.py` and `foundation.py` passed against PostgreSQL, including actual create/readback and old/new recipient serialization. | Proven |
| Migration, references, old defaults and fixtures | `tools/delivery/foundation.py` applies 0027 over historical confirmed data, checks nullable initial selection, snapshot/reference protection, retirement and downgrade/re-upgrade. Fresh run exited 0. | Proven |
| Retirement, booked timezone and action period | `tools/delivery/foundation.py`, `concurrency.py` and `form_configuration.py` prove booked-window/timezone protection, real concurrent booking/retirement in both channels, and rejecting action-period edits that exclude configured dates. Fresh run exited 0. | Proven |
| Authorized schedule read/write and revision conflicts | `foundation.py` exercises real role principals and repository revision races; recorded admin gate `...admin-20260906l` proves actual competing HTTP writes and both UI reconciliation choices. | Proven |
| Delivery section in management UI | Current `manage-action.tsx` mounts `DeliverySection`; recorded real admin browser gates prove persisted editing, tab retention, 3/3/1 windows and responsive rendering. | Proven |
| Historical read and non-delivery regression | `foundation.py` reads a valid pre-0027 confirmed order through the actual repository after migration with unchanged buyer/line/total and null delivery. `form_configuration.py` creates, reads and replays a sponsoring order without delivery. Both passed in the latest isolated run. | Proven |
| Admin effective form and empty availability | Core-backed summary shows required fields, optional limits and future selectable counts; real browser tests verify zero-to-one recovery and seven windows after Admin save. Prior admin lifecycle tests prove add/edit/copy/retire and conflict reconciliation. | Proven |
| Same-action operational configuration propagation | Real Admin UI saves 3/3/1 windows; already-loaded Anna/public forms refresh to identical saved IDs without rebuild or CMS publication. Both channels now create real orders afterward; admin API and PostgreSQL prove matching delivery snapshots and separate/reused billing. Same-action retirement now rejects both loaded selections, retains inputs and creates no orders until explicit replacement. Admin UI now verifies both persisted orders and keyboard disclosure operation; EmDash continuation remains open. | Partial |
| Template/default compatibility across channels | Persisted Krapfentaxi/blank defaults and existing null records are proven. `form_configuration.py` now also creates and reads a real review-ready sponsoring order on a blank action, preserving EUR 5 pricing and billing with null delivery fields; exact replay returns the same order. Shared Core projections and persisted defaults were re-read and the foundation gate rerun successfully; campaign-renderer compatibility remains under EmDash acceptance below. | Proven for existing channels |
| Both capture channels and legacy completion | Actual acquisition/public orders, address reuse/separation, notes/contact, country fields, historical completion and invoice issuance have separate proofs in PROGRESS.md. The same-action 3/3/1 journey now includes both real orders, stale-selection recovery and administrator review; actual legacy completion-to-invoice is separately proven. The additional campaign renderer remains under EmDash acceptance below. | Proven for existing channels |
| Recovery behavior | Public stale policy/window, empty availability, no-JavaScript error retention and accepted-response retry; Anna accepted-response 503 retry; admin revision reconciliation all have recorded live proofs. Buyer-switch and action-switch reset now have actual browser proofs. No-JavaScript history retry now returns the original accepted reference without duplication, but history restores an earlier field value and requires re-entering the last correction. Direct no-JavaScript reload now proves a byte-identical POST, preserving every latest input and returning the original reference. The earlier-state limitation is specific to browser history Back; A direct In-App save now persists a deferred-delivery draft with null delivery/window/invoice snapshots, confirmed in PostgreSQL. Administrator completion now also has a real accepted-update/503/retry browser proof: changed payload is blocked, exact retry keeps its key and stored completion version, then closes with focus restored. Initial completion-context load failure now has a real explicit-retry proof. These results cover existing channels; repeat them through the EmDash transport when available. | Proven for existing channels; history limitation recorded |
| Privacy handling of new delivery fields | Real privacy browser flow and PostgreSQL assertion remove contactName, contactPhone and instructions; existing retained invoice/document hashes stay unchanged. See privacy checkpoint in PROGRESS.md. | Proven |
| Final In-App Browser acceptance | Direct public desktop In-App screenshots and billing-toggle/notes/refresh interactions now pass; corrected the refresh button found there. Authenticated In-App dashboard inspection now covers 360/390/430px: a single beneficiary adds 60px, and multiple beneficiaries expand. A 200% text overflow was found and corrected. Anna's form now also has direct 360/390/430px normal/200% measurements and a real deferred-draft save/readback. Public mobile In-App ordering now persists separate billing, contact, multiline notes and an explicit window, verified in PostgreSQL. Its discovered 200% text overflow is corrected and fresh-page measurements pass at all three widths. Integrated EmDash acceptance remains open. | Partial |
| EmDash canonical/alias parity and editorial independence | Read-only parallel worktree baseline `67d64b1` adds scoped Charity editorial workflows; its `apps/campaign-site/src` still has no public order renderer, and EMS-050 remains unchecked. No source was imported or modified there. | Open |

Fresh foundation command:

```sh
LEONAID_DELIVERY_FOUNDATION_SUBNET=172.30.81.0/24 sh tools/delivery/test-foundation.sh
```

The run used a newly created internal network, temporary PostgreSQL storage and no host ports; cleanup targeted only returned resource IDs. The default automatic subnet attempt failed before starting a database because Docker's address pools were exhausted. Optional explicit subnet support was added and the completed run passed every foundation/order/race/completion/template check. No existing Docker network was removed or changed.

## Remaining integrated acceptance

The dependency was revalidated against the actual parallel source and EMS-050 checklist at `67d64b1`, not inferred from its commit title. No campaign renderer or serving-app order adapter is available to integrate yet. Do not replace this requirement with another legacy-renderer run or copy an unfinished CMS implementation into this branch.

When the agreed EmDash baseline provides EMS-050/085:

1. Reconcile Core canonical resolution and the serving application's Astro Action/API transport. Wire the existing authoritative delivery definition and fields into the actual campaign form. Order POSTs must not redirect.
2. On one synthetic action, repeat the saved 3/3/1 schedule journey through `/campaigns/<archive_slug>/` and its Core-managed alias, with and without JavaScript. Verify separate/reused billing, contact, instructions and window snapshots in Admin and PostgreSQL.
3. Retire a loaded selection and change operational rules without CMS publication or site rebuild. Verify rejection without a write, input retention, explicit reselection and exact-retry/edited-submission command behavior through the new transport.
4. Publish editorial changes and prove they cannot change delivery rules or order data. Verify Core withdrawal, consent/version and origin protections, and renderer rollback with accepted Core orders retained.
5. Re-run relevant integration, accessibility and mobile checks on the combined baseline, then complete the remaining PLAN.md items and this audit. Existing successful checks are prerequisites, not proof of the new renderer.

Existing-channel evidence is now current through public field accessibility (`b769c92`), completion recovery (`927f1ba`), PWA/actions regression and 213 unit tests. The no-JavaScript browser-history Back limitation remains explicitly documented: Back may restore an earlier validation form; direct reload/retry preserves the entire latest POST and does not duplicate the order. No claim of browser-wide history restoration is made.
