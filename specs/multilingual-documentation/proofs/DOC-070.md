# DOC-070 – Getrenntes Hosting und öffentliche Abnahme

Status: **öffentlich abgenommen; Nachtlauf und Rückweg offen**

Vorbereitungsrevision: `045c68e71c421ccc49ecb5694cdeb0b5a4acee35`

Initialer PR: [#8](https://github.com/BjoernSchotte/leonaid/pull/8), gemergt
als `9e68f92030940f4aec453e2df87bc8911d68c71d`.

CI-Läufe:
[Documentation 34887093813](https://github.com/BjoernSchotte/leonaid/actions/runs/34887093813),
[Documentation 34888145440](https://github.com/BjoernSchotte/leonaid/actions/runs/34888145440)

## Vorbereiteter Veröffentlichungsvertrag

- Ziel ist die statische GitHub-Pages-Site
  `https://bjoernschotte.github.io/leonaid/` mit HTTPS und Base-Path
  `/leonaid`.
- Der Build läuft mit read-only Repository-Rechten. Nur der nachgelagerte,
  `main`-gebundene Deployment-Job erhält `pages: write` und `id-token: write`.
- Ein normaler `main`-Push baut und prüft nur. Ausschließlich der Nachtlauf und
  ein manueller Wiederanlauf vom `main`-Ref dürfen das Pages-Artefakt
  bereitstellen und veröffentlichen.
- `actions/upload-pages-artifact` und `actions/deploy-pages` sind auf volle
  Commit-SHAs fixiert. Das Deployment übernimmt ausschließlich
  `apps/docs/dist` und wird über die `github-pages`-Concurrency serialisiert.
- Nach der atomaren Aktivierung prüft `./leonaid test-docs` die live
  ausgelieferte Manifest-SHA sowie DE/EN, Suche und Sprachwechsel.
- Der getrennte `Documentation watchdog` beobachtet fehlgeschlagene
  `main`-Läufe sofort und prüft alle sechs Stunden die letzte erfolgreiche
  Veröffentlichung. Nach 30 Stunden öffnet er ein Repository-Issue; ein
  erfolgreicher Wiederanlauf schließt es. Seine Rechte sind auf
  `actions: read` und `issues: write` begrenzt.
- Canonicals, Sprachalternativen, Sitemap, `robots.txt`, Betreiber- und
  Datenschutzhinweise sind auf das gewählte Ziel vorbereitet. Der bestehende
  Lizenzstatus wird ausdrücklich beibehalten.

## Lokaler Nachweis

Gezielte Prüfungen:

```text
node apps/docs/scripts/check-workflow.mjs
docs-workflow: OK: shared gates, main-only Pages deployment, live smoke and independent publication watchdog

validateContent(...)
docs-content: OK: 62 published pages, DE/EN pairs and links valid

node apps/docs/scripts/check-product-contracts.mjs
docs-product-contracts: OK: 25 user and technical pages match current code and contract tokens
```

Pages-konfigurierter Build und Browser-Smoke:

```text
LEONAID_DOCS_SITE_URL=https://bjoernschotte.github.io \
LEONAID_DOCS_BASE_PATH=/leonaid ./leonaid docs-build
docs-build-output: OK: 164 allowlisted static files; manifest, routes, anchors,
assets, search, sitemap, canary and draft exclusion valid

LEONAID_DOCS_SITE_URL=https://bjoernschotte.github.io \
LEONAID_DOCS_BASE_PATH=/leonaid \
LEONAID_DOCS_EXPECTED_REVISION=975223ec2789ff7df743d4593275a351506d1266 \
./leonaid test-docs
4 passed
```

Das lokale Manifest nannte `https://bjoernschotte.github.io/leonaid/`, 62
veröffentlichte Seiten, 31 Sprachpaare und die erwartete Quell-SHA. Nach dem
Smoke blieben keine `leonaid-docs`-Container oder -Netzwerke zurück.

## CI-Nachweis

Der PR-Lauf `34887093813` bestand Inhalts-, Übersetzungs-, Link-, Build- und
Chromium-Gates in 1:24 Minuten. Sein Artefakt `docs-site-34887093813-1`
enthält das vollständige Pages-Ziel. Das Manifest bindet die vom PR-Lauf
geprüfte Merge-Revision `7aa11919fc3c285ba80d8a3d6db92f01ccb1b590`, den
Inhaltshash `9983c7bf1b474b83d550066f5cc3f44579654120947b96ac42641757f534bcd8`
und die öffentliche Ziel-URL. Das Browserartefakt enthält drei Screenshots;
die Accessibility-Auswertung enthält keine Verstöße. Pages-Upload und
Deployment wurden im PR erwartungsgemäß übersprungen.

Der nach der Ereignis-Korrektur gestartete Lauf `34888145440` bestand dieselben
Gates in 1:57 Minuten. Damit ist zusätzlich geprüft, dass `main`-Pushes nur das
Artefakt erzeugen und ausschließlich Nachtlauf oder manueller Wiederanlauf den
Pages-Upload und das Deployment freischalten.

## Erste öffentliche Veröffentlichung

GitHub Pages wurde am 14.09.2026 als öffentliche Actions-Site mit erzwungenem
HTTPS aktiviert. Der manuelle `main`-Lauf
[Documentation 34893526348](https://github.com/BjoernSchotte/leonaid/actions/runs/34893526348)
veröffentlichte den geprüften Build und bestand den nachgelagerten Live-Smoke.
Das ausgelieferte `build-manifest.json` nennt:

```text
artifactId: gha-34893526348-1
sourceSha: 9e68f92030940f4aec453e2df87bc8911d68c71d
contentSha256: b959db4be010a09e75e69e6b52f602041651497661777ced1624ee3e57950a4c
publicUrl: https://bjoernschotte.github.io/leonaid/
publishedPages: 62
languagePairs: 31
```

Der Actions-Live-Smoke prüfte anonym über HTTPS die DE/EN-Einstiege, konkrete
Seiten aller drei Bereiche, Assets, Suche, Sprachwechsel und exakt diese
Manifest-SHA. Der englische Root-README verlinkt nun zusätzlich die bestätigte
Site-URL und behält alle direkten Docs-Quelllinks.

## Noch offene Nachweise

1. Den tatsächlichen geplanten Nachtlauf beobachten und einen kontrolliert
   abgebrochenen `main`-Lauf samt Watchdog-Alarm und Wiederanlauf prüfen.
2. Einen fehlgeschlagenen Live-Smoke kontrolliert auslösen, das vorige bekannte
   gute Pages-Artefakt erneut veröffentlichen und die Rückkehr anhand der
   Manifest-SHA prüfen.
