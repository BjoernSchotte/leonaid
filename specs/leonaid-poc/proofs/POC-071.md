# POC-071 – Hochwertige Astro-Aktionsseite

Stand: vollständig bewiesen

## Ergebnis

Die öffentliche Aktionsseite ist eine eigenständige, serverseitig gerenderte
Astro-7-Anwendung. Sie macht eine veröffentlichte Charity-Aktion ohne Login
verständlich: Zweck, Zeitraum, Träger, Begünstigte, Fortschritt, aktive
Angebote und Datenschutzinformation sind auf einer responsiven Seite
zusammengeführt.

Die Gestaltung verwendet die Lions-Farben Blau und Gold, lokal ausgelieferte
variable Schriften und ein eigens generiertes, textfreies Gemeinschaftsmotiv.
Die visuelle Hierarchie führt vom Aktionszweck über das konkrete Angebot zur
lokalen Wirkung und zum gemeinsamen Ziel. Alias-, Archiv-, Inaktiv-,
Nicht-gefunden- und Ausfallzustand besitzen jeweils eine eigenständige,
hilfreiche Darstellung.

Damit setzt der Baustein den Public-Web-Schnitt aus Kapitel 16 sowie die
Qualitäts- und Entkopplungsentscheidungen aus Kapitel 19 des
[Produkt- und Architekturvorschlags](../../produkt-und-architekturvorschlag.md)
um.

## Klare fachliche Grenze

FastAPI bleibt die einzige Quelle für Fachentscheidungen. Der Core entscheidet:

- ob eine Alias-Aktion innerhalb ihres Publikationsfensters sichtbar ist;
- ob eine Route veröffentlicht, inaktiv oder ein unveränderliches Archiv ist;
- welche Angebote den Status `active` besitzen und öffentlich erscheinen;
- ob schreibende Vorgänge grundsätzlich zulässig sind.

Die Astro Actions validieren nur Transportdaten, rufen den generierten
OpenAPI-Client auf und übersetzen technische Nicht-gefunden- beziehungsweise
Ausfallzustände. Sie enthalten keine Publikations-, Preis-, Angebots- oder
Bestelllogik. Das mit POC-072 ergänzte öffentliche Bestellformular erfasst
zusätzlich Spam-Signale und leitet den Auftrag an FastAPI weiter.

Der reale Vertragstest liest dieselbe aktive, inaktive und archivierte Route
jeweils einmal direkt aus dem FastAPI-Core und einmal aus dem Astro-Renderer.
Status, Aktion, Canonical URL und öffentliche Angebote stimmen überein. Ein
inaktiver Alias gibt keine früheren Aktionsdaten preis.

## Rendering, Metadaten und Auslieferung

Archiv-, Inaktiv- und reine Zustandsseiten funktionieren ohne ausgeliefertes
Client-JavaScript. Nur eine veröffentlichte Aktion mit aktivem Bestellformular
lädt die gezielte Formularverbesserung für Preisvorschau, Eingabe-Erhalt und
Fokusführung. Der Regressionstest prüft beide Grenzen. Schriften, CSS, Favicon,
WebP-Herobild und Social-Preview-PNG werden aus dem eigenen Public-Container
ausgeliefert.

Jede Seite pflegt:

- deutschen Titel und Beschreibung;
- Canonical URL mit korrektem HTTPS-Protokoll hinter dem Reverse Proxy;
- Robots-Regel passend zum fachlichen Zustand;
- Open-Graph- und Twitter-Metadaten einschließlich Bildbeschreibung;
- kurze Cache-Regeln für aktive Aliase, längere Regeln für Archive und
  `no-store` im Ausfallzustand.

Die Containerstufe baut und typprüft Astro mit exakt gepinnten Abhängigkeiten
und startet das SSR-Ergebnis anschließend als unprivilegierter `node`-Nutzer.

## Reale Teststrecke

Der vollständige Nachweis läuft mit:

```sh
./leonaid test-public-actions
```

Der Lauf startet alle benötigten Dienste aus leeren projektspezifischen
Volumes, migriert eine echte PostgreSQL-Instanz, lädt Golden Data, erzeugt
einen veröffentlichten Folgejahrgang und aktiviert dessen öffentliches
Krapfenbox-Angebot. Es gibt keine Request-, Repository-, Zeit-, Datenbank-
oder Browser-Mocks.

Bewiesen werden:

- reine Domain- und Angebotsregeln unabhängig von Astro;
- Core/Astro-Parität für aktive, inaktive und archivierte Routen;
- Krapfenbox mit 24 Stück, 36,00 EUR und ausschließlich aktivem Status;
- unveränderte Alias- und Archivregeln aus POC-070;
- aktive, inaktive und archivierte Seite in Chromium, Firefox und WebKit;
- kein horizontaler Scroll von 320 bis 1.440 CSS-Pixeln;
- mindestens 44 × 44 CSS-Pixel große sichtbare Interaktionsziele;
- Tastatur-Einstieg über den Skip-Link;
- korrekte Canonical- und Social-Metadaten hinter dem TLS-Proxy;
- gezieltes Client-JavaScript ausschließlich bei aktivem Formular.

Ergebnis:

```text
public-actions-contract: atomarer Folgejahrwechsel, neutrale Alias-Sicht,
dauerhafte Archive, Astro/Core-Parität, Angebote und Schreibschutz real
bewiesen
4 passed, 2 skipped
1 passed
public-actions-test: OK: Astro/Core-Parität, drei Browser, responsive Public
UX, Alias/Archiv, WCAG 2.2 AA und Web-Vitals real bewiesen
```

Die zwei übersprungenen Fälle sind bewusst die doppelte visuelle
Randbreitenprüfung in Firefox und WebKit. Alle drei Browser führen den
vollständigen fachlichen Aktiv-/Inaktiv-/Archivablauf aus; die zusätzlichen
320-/1.440-Pixel-Screenshots werden einmal in Chromium archiviert.

## Accessibility und Performance

Der automatisierte Axe-Lauf gegen die aktive mobile Golden-Seite meldet für
WCAG 2.2 AA:

```text
critical/serious findings: 0
total violations: 0
```

Das lokal reproduzierbare Performanceprofil verwendet Chromium 138 über
Playwright 1.54.1, 390 × 844 CSS-Pixel, vierfache CPU-Drosselung, 40 ms
Netzlatenz, 10 Mbit/s Download und 5 Mbit/s Upload. Der archivierte finale
Lauf wird aus `.artifacts/poc071/performance-report.json` übernommen:

```text
LCP: 268 ms, Budget <= 2.500 ms
INP: 24 ms, Budget <= 200 ms
CLS: 0,0419, Budget <= 0,1
```

Die maschinenlesbaren Berichte liegen lokal unter:

- `.artifacts/poc071/accessibility-report.json`
- `.artifacts/poc071/performance-report.json`

## Sichtbarer Browsernachweis

Zusätzlich zur Playwright-Matrix wurde die reale Anwendung im
Codex-In-App-Browser geprüft. Der sichtbare Durchlauf bestätigt Struktur,
Typografie, Angebot, Begünstigtenbereich, Ziel, Datenschutz und
Archivhinweis.

Automatisierte Screenshots:

- `.artifacts/poc071/public-active-chromium-390.png`
- `.artifacts/poc071/public-active-chromium-1440.png`
- `.artifacts/poc071/public-active-firefox-390.png`
- `.artifacts/poc071/public-active-webkit-390.png`
- `.artifacts/poc071/public-archive-chromium-1440.png`
- `.artifacts/poc071/public-inactive-chromium-390.png`

Das Motiv wurde mit der integrierten Bildgenerierung als textfreie,
editoriale Papiercollage in Lions-Blau, Lions-Gold und warmem Elfenbein
erzeugt. Die finalen Dateien liegen unter
`apps/public/public/community-routes.png` und
`apps/public/public/community-routes.webp`.

## Qualitätsnachweise

- Domain-Unit-Tests: `92 passed`.
- Bestehender Authentifizierungs- und Adminpfad gegen den neuen
  Public-Container: Session-Vertrag grün und Chromium-E2E `1 passed` für
  E-Mail-Code, 90-Tage-Sitzung, Fresh Login, echte Einladung und Logout.
- Astro Check: 16 Dateien, 0 Fehler, 0 Warnungen, 0 Hinweise.
- Produktionsbuild im gepinnten Public-Dockerfile erfolgreich.
- Ruff, mypy, OpenAPI-Synchronität, generierter TypeScript-Client,
  TypeScript-Typecheck, Prettier und `git diff --check` ohne Befund.

## Krapfentaxi-Vorlagenlayout (3. September 2026)

Auf Nutzerwunsch wurde ausschließlich die öffentliche Frontend-Darstellung
an [lions-krapfentaxi.de](https://lions-krapfentaxi.de/) angelehnt:
dunkle Abschnittsnavigation, flächige Illustration, Krapfentaxi-Logo,
zentrierter Einstieg mit Bestelllink, wechselnde Bild-/Textbereiche,
Bäckerei-Abschnitt und helle Begünstigten-Karten. Die Bildquellen und der
Rechtestatus stehen in
`apps/public/src/assets/krapfentaxi/SOURCES.md`. Astro liefert die lokalen
Bilder optimiert aus; PHP, Bootstrap und Skripte der Quellseite wurden
nicht übernommen.

Die Darstellung ist auf Aktions-Archiv-Slugs `krapfentaxi` und
`krapfentaxi-*` begrenzt. Andere Aktionen und neutrale Zustandsseiten
behalten ihre bisherige Darstellung. Jahr, Preise, Boxinhalt, Zeitraum,
Träger und Begünstigte kommen unverändert vom Core. Es wurden keine
Backendfunktionen, API-Verträge, Migrationen oder Golden-Daten geändert.
Insbesondere bleibt das bestehende LeonAid-Bestellformular erhalten;
Liefergebietsprüfung, mehrere Lieferadressen und Terminauswahl aus der
Vorlage sind nicht Bestandteil dieser Änderung.

Erneut ausgeführte Nachweise:

- `./leonaid test-public-orders`: echte Core-/Twenty-Verträge und alle
  drei Browser-Bestellwege mit tatsächlicher Persistenz grün.
- `./leonaid test-public-actions`: Core-/Astro-Parität grün;
  Chromium, Firefox und WebKit grün (4 bestanden, 2 bewusst übersprungene
  doppelte Randbreitenprüfungen); 320 bis 1440 Pixel ohne horizontalen
  Überlauf, Archiv ohne Bestellformular, lokale Bilder und Metadaten geprüft.
- Mobiler Axe-Lauf: 0 Verstöße. Gemessene Werte im oben beschriebenen
  Laborprofil: LCP 252 ms, INP 16 ms, CLS 0,0003. Kein Produktions-Feldnachweis.
- Astro Check: 20 Dateien, 0 Fehler, 0 Warnungen, 0 Hinweise;
  gepinnter Docker-Produktionsbuild, Prettier und Diff-Prüfung grün.
- Sichtbarer In-App-Browser: Einstieg, Inhaltsbereich, Navigation zum
  vorhandenen Formular und Preisvorschau (2 Boxen = 72,00 EUR) geprüft.
  Mobile Browser-Screenshots wurden zusätzlich visuell kontrolliert.

Die beiden isolierten Suiten wurden nacheinander ausgeführt, nachdem ein
paralleler Start das verfügbare Docker-Netzwerkadresskontingent ausschöpfte.
Keine fremden Netzwerke oder produktiven Daten wurden dafür gelöscht.
Die oben dokumentierten historischen Vollprüfungen wurden nicht komplett
wiederholt. Die aktuellen Screenshots und Berichte liegen weiterhin unter
`.artifacts/poc071/` beziehungsweise `.artifacts/poc072/`.
